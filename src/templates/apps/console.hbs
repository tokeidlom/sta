<div class="console-container">

      <nav class="sheet-tabs tabs">
        <a class="item {{tabs.crew.cssClass}}" data-group="primary" data-action="tab" data-tab="crew">{{localize 'sta.actor.character.character'}}</a>
        <a class="item {{tabs.ships.cssClass}}" data-group="primary" data-action="tab" data-tab="ships">{{localize 'sta.actor.character.traits'}}</a>
        <a class="item {{tabs.development.cssClass}}" data-group="primary" data-action="tab" data-tab="development">{{localize 'sta.actor.character.development'}}</a>
        <a class="item {{tabs.notes.cssClass}}" data-group="primary" data-action="tab" data-tab="notes">{{localize 'sta.actor.note.title'}}</a>
      </nav>

      <section class="sheet-body">
        <div class="tab {{tabs.crew.cssClass}}" name="crew" data-group="primary" data-tab="crew">
          <!-- Content for crew -->
crew

        </div>
        <div class="tab {{tabs.ships.cssClass}}" name="ships" data-group="primary" data-tab="ships">
          <!-- Content for crew -->
ships

        </div>
        <div class="tab {{tabs.development.cssClass}}" name="development" data-group="primary" data-tab="development">
          <!-- Content for crew -->
development

        </div>
        <div class="tab {{tabs.notes.cssClass}}" name="notes" data-group="primary" data-tab="notes">
          <!-- Content for crew -->
notes

        </div>
      </section>
</div>